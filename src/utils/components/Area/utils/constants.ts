import { ONE_HOUR } from '@utils/utils/time';

export const DEFAULT_PROMETHEUS_SAMPLES = 60;
export const DEFAULT_PROMETHEUS_TIMESPAN = ONE_HOUR;

export const LAST_LANGUAGE_LOCAL_STORAGE_KEY = 'bridge/last-language';

export const DEFAULT_HEIGHT = 180;
export const DEFAULT_TICK_COUNT = 2;
